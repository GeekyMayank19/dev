let arr = [20, 30, 80, 100, 40];
// displayArray(arr);

function displayArray(arr){
    console.log(arr + " = " + arr.length);
}

// arr[10] = "50000";
// arr["kuchbhi"] = 1010;
// console.log(arr["kuchbhi"]);
 


// arr.unshift(12);

// question rearrange array so that 
//all even elements at font and all
// odd elements are at back
arr.shift();
displayArray(arr);